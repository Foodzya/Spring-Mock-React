import React from 'react';

const LandingInfo = () => {
    return (
        <div className='landing-container'>
            <h1 className="landing-header">Projects</h1>
            <p className="landing-paragraph">From configuration to security, web apps to big data—whatever the infrastructure needs
                of your application may be, there is a Spring Project to help you build it. Start small and use just what you
                need—Spring is modular by design.</p>
        </div>
    )
}

export default LandingInfo;